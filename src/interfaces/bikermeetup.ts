import { Interface } from "readline";

export interface BikerMeetup {
    id: string,
    name: string,
    date: string,
    desc: string,
    xValue: number,
    yValue: number,
    createdAt: string,
    createdBy: string,
}